import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF3B82F6);
  static const secondary = Color(0xFF06B6D4);
  static const background = Color(0xFF0A0F1E);
  static const surface = Color(0xFF1A2235);
  static const surfaceVariant = Color(0xFF243047);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF94A3B8);
}

class AppStrings {
  static const appName = 'RainCheck';
  static const tagline = 'Parametric Insurance for Riders';
}

class AppSizes {
  static const borderRadius = 12.0;
  static const cardRadius = 16.0;
  static const padding = 16.0;
}
